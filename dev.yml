version: '3'
services:
  postgres:
    image: postgres
  django:
    build:
      context: .
      dockerfile: ./compose/django/Dockerfile
    command: python manage.py runserver 0.0.0.0:8000
    ports:
      - "8000:8000"
    volumes:
      - .:/code
      - /Users/prodriguez/projects/django-trench:/code-trench
    depends_on:
      - postgres